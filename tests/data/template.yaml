data:
  - title: Normal status
    options: unbound-control command options
    raw: |
      # unbound-control response
    expected: |
      # unbound-control-ts json reponse
    exception: "# exception message"
